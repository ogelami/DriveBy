#!/usr/bin/env python3

from DriveBy import upload
import argparse

parser = argparse.ArgumentParser()
parser.add_argument('file', default=1, metavar='HOSTNAME', help='Hostname or ip of the target host')

args = parser.parse_args()

print(upload(args.file))
