#!/usr/bin/env python3

from DriveBy import upload
import argparse

parser = argparse.ArgumentParser()
parser.add_argument('file', default=1, metavar='FILE', help='Name of the file to upload')

args = parser.parse_args()

print(upload(args.file))
